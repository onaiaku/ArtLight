# Vibepollo 1.16.0-beta.13 - 2026-06-05

## Fixes
- Fixed Windows release installer packaging so debug sections are stripped from shipped binaries.
- Filled missing web UI locale keys so translated settings pages showed complete labels.
- Fixed multi-client virtual display stalls when per-client display cleanup overlapped with another active session.
- Fixed paired-client permission checks for GameStream requests that reused a remembered TLS identity after the original certificate context was no longer active.
