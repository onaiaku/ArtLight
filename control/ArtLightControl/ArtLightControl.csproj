<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <!-- Stesso TFM di Core: garantisce accesso alle stesse API WinRT -->
    <TargetFramework>net8.0-windows10.0.19041.0</TargetFramework>
    <EnableWindowsTargeting>true</EnableWindowsTargeting>
    <TargetPlatformMinVersion>10.0.19041.0</TargetPlatformMinVersion>
    <RootNamespace>ArtLightControl</RootNamespace>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <!-- WinUI 3 -->
    <UseWinUI>true</UseWinUI>
    <!-- Unpackaged: distribuzione via InnoSetup, non MSIX -->
    <WindowsPackageType>None</WindowsPackageType>
    <Platforms>x64</Platforms>
    <RuntimeIdentifier>win-x64</RuntimeIdentifier>
    <ApplicationIcon>Resources\artlightcontrol.ico</ApplicationIcon>
    <ApplicationManifest>app.manifest</ApplicationManifest>
    <!-- The file name stays ArtLightControl: the LocalSystem service only accepts pipe clients
         called ArtLightControl.exe from its own install directory (§22), so renaming the binary
         would silently break every NIC change. What the taskbar, Alt-Tab and Task Manager show
         is FileDescription, which .NET fills from AssemblyName unless told otherwise — hence
         these two, which change the name without touching the file. -->
    <AssemblyName>ArtLightControl</AssemblyName>
    <AssemblyTitle>ArtLightControl</AssemblyTitle>
    <Product>ArtLightControl</Product>
    <Company>onaiaku</Company>
    <!-- Keeps the compiler-generated Main (renamed to XamlGeneratedProgram.XamlGeneratedMain)
         but stops it being the entry point, so Program.cs can opt into the WinAppSDK 2.3
         XAML performance changes before Application.Start. See Program.cs. -->
    <DefineConstants>$(DefineConstants);DISABLE_XAML_GENERATED_MAIN</DefineConstants>
    <Version>1.0.0</Version>
    <AssemblyVersion>1.0.0.0</AssemblyVersion>
    <FileVersion>1.0.0.0</FileVersion>
  </PropertyGroup>

  <!-- dotnet publish on this unpackaged WinUI app silently omits the compiled XAML
       resources (.xbf files and the .pri) from the publish output. The exe builds and
       runs, then crashes at startup with 0xc000027b in Microsoft.UI.Xaml.dll because
       App.xbf is missing. Visual Studio's build flow copies them; the bare CLI does not.
       This target restores parity: after publish, copy every .xbf (preserving the
       Controls/Themes/Views subfolders) plus the app .pri into the publish directory. -->
  <Target Name="CopyWinUIResourcesToPublish" AfterTargets="Publish">
    <ItemGroup>
      <XbfFiles Include="$(OutputPath)**\*.xbf" />
      <AppPriFiles Include="$(OutputPath)$(AssemblyName).pri" />
    </ItemGroup>
    <Copy SourceFiles="@(XbfFiles)"
          DestinationFiles="@(XbfFiles->'$(PublishDir)%(RecursiveDir)%(Filename)%(Extension)')"
          SkipUnchangedFiles="true" />
    <Copy SourceFiles="@(AppPriFiles)"
          DestinationFolder="$(PublishDir)"
          SkipUnchangedFiles="true" />
  </Target>

  <ItemGroup>
    <None Remove="Resources\paypal.svg" />
    <None Remove="Resources\nvidia-eye.svg" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\ArtLightControl.Core\ArtLightControl.Core.csproj" />
  </ItemGroup>
  <ItemGroup>
    <PackageReference Include="Microsoft.WindowsAppSDK" Version="2.3.1" />
    <PackageReference Include="H.NotifyIcon.WinUI" Version="2.1.0" />
  </ItemGroup>

  <ItemGroup>
    <!-- Custom fonts -->
    <Content Include="Resources\DMSans-Regular.ttf">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </Content>
    <Content Include="Resources\DMSans-Medium.ttf">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </Content>
    <Content Include="Resources\DMSans-SemiBold.ttf">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </Content>
    <Content Include="Resources\JetBrainsMono-Regular.ttf">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </Content>
    <Content Include="Resources\JetBrainsMono-Medium.ttf">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </Content>
    <Content Include="Resources\paypal.svg">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </Content>
    <Content Include="Resources\nvidia-eye.svg">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </Content>
    <!-- Custom host tile PNGs deployed to <HostInstallDir>\assets\ on demand -->
    <Content Include="Resources\desktop.png">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </Content>
    <Content Include="Resources\steam.png">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </Content>
  </ItemGroup>

  <ItemGroup>
    <!-- ICO files: needed at runtime for tray icon and window icon -->
    <Content Include="Resources\artlightcontrol.ico">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </Content>
    <Content Include="Resources\streammodeko.ico">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </Content>
    <Content Include="Resources\streammodeok.ico">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </Content>
    <!-- Store badge SVGs for Game Library tab -->
    <Content Include="Resources\Badges\store_battlenet.svg">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </Content>
    <Content Include="Resources\Badges\store_ea.svg">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </Content>
    <Content Include="Resources\Badges\store_epic.svg">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </Content>
    <Content Include="Resources\Badges\store_gog.svg">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </Content>
    <Content Include="Resources\Badges\store_steam.svg">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </Content>
    <Content Include="Resources\Badges\store_ubisoft.svg">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </Content>
    <Content Include="Resources\Badges\store_xbox.svg">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </Content>
  </ItemGroup>

</Project>
