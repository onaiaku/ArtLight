<Page
    x:Class="ArtLightControl.Views.ClientsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:ArtLightControl.ViewModels"
    xmlns:ctrl="using:ArtLightControl.Controls"
    Background="{StaticResource STBackground}">

    <Grid Margin="28,24,28,28">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Style="{StaticResource ST8_PanelRoot}">

                <StackPanel Spacing="3">
                    <TextBlock Text="Clients" Style="{StaticResource ST8_PanelTitle}"/>
                    <TextBlock Text="Approved ArtMoon devices that may read metrics and drive the host."
                               Style="{StaticResource ST_PageSubtitle}"/>
                </StackPanel>

                <!-- Clipboard shared with ArtMoon (8.7.0, §79). Above the list it applies to:
                     one switch for every approved device. -->
                <StackPanel Spacing="8">
                    <TextBlock Text="CLIPBOARD" Style="{StaticResource ST_SectionLabel}"/>
                    <Border Style="{StaticResource ST8_GroupCard}">
                        <Border Style="{StaticResource ST8_Row}" BorderThickness="0">
                            <Grid>
                                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Spacing="3" VerticalAlignment="Center" Margin="0,0,16,0">
                                    <StackPanel Orientation="Horizontal" Spacing="7">
                                        <TextBlock Text="Share clipboard with ArtMoon" Style="{StaticResource ST_RowLabel}" VerticalAlignment="Center"/>
                                        <ctrl:InfoHint Term="Shared clipboard" Tip="Text copied on this PC can be pasted on the device, and the other way round, while it streams. Encrypted with a key that lasts one stream. Passwords are cleared within 60 seconds. The device must turn it on too."/>
                                    </StackPanel>
                                    <TextBlock Text="Approved devices, while streaming" Style="{StaticResource ST_RowDescription}"/>
                                </StackPanel>
                                <ToggleSwitch Grid.Column="1" Style="{StaticResource ST_Toggle}" VerticalAlignment="Center"
                                              AutomationProperties.Name="Share clipboard with ArtMoon"
                                              IsOn="{x:Bind ViewModel.ShareClipboard, Mode=TwoWay}"/>
                            </Grid>
                        </Border>
                    </Border>
                </StackPanel>

                <StackPanel Spacing="8">
                    <StackPanel Orientation="Horizontal" Spacing="7">
                        <TextBlock Text="ARTMOON DEVICES" Style="{StaticResource ST_SectionLabel}" VerticalAlignment="Center"/>
                        <ctrl:InfoHint Term="Client approval" Tip="Only approved ArtMoon devices can use the advanced integration (host metrics, link-speed switch, power-off, …); streaming itself is never affected. New devices request access on first connection — approve them once here. Requires ArtMoon 3.2.0 or later."/>
                    </StackPanel>

                    <ItemsControl ItemsSource="{x:Bind ViewModel.BridgeClients, Mode=OneWay}">
                        <ItemsControl.ItemContainerStyle>
                            <Style TargetType="ContentPresenter">
                                <Setter Property="HorizontalAlignment" Value="Stretch"/>
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            </Style>
                        </ItemsControl.ItemContainerStyle>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate x:DataType="vm:BridgeClientItem">
                                <Border Style="{StaticResource ST8_GroupCard}" Padding="18,12" Margin="0,0,0,8">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0" Spacing="6" VerticalAlignment="Center" Margin="0,0,16,0">
                                            <TextBlock Text="{x:Bind Name}" Style="{StaticResource ST_RowLabel}"/>
                                            <Border HorizontalAlignment="Left" CornerRadius="4" BorderThickness="1" Padding="8,2"
                                                    Background="{x:Bind StatusBgHex, Converter={StaticResource HexToSolidColorBrushConverter}}"
                                                    BorderBrush="{x:Bind StatusBorderHex, Converter={StaticResource HexToSolidColorBrushConverter}}">
                                                <TextBlock Text="{x:Bind StatusLabel}" FontFamily="{StaticResource DMSansSemiBold}" FontSize="11"
                                                           Foreground="{x:Bind StatusColorHex, Converter={StaticResource HexToSolidColorBrushConverter}}"/>
                                            </Border>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                            <Button Content="Approve" Style="{StaticResource ST_ButtonGreen}" Tag="{x:Bind UniqueId}" Click="ApproveClient_Click"
                                                    Visibility="{x:Bind CanApprove, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                            <Button Content="Revoke" Style="{StaticResource ST_ButtonDanger}" Tag="{x:Bind UniqueId}" Click="RevokeClient_Click"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <Border Style="{StaticResource ST8_GroupCard}"
                            Visibility="{x:Bind ViewModel.HasNoBridgeClients, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                        <Border Style="{StaticResource ST8_Row}" BorderThickness="0">
                            <TextBlock Text="No devices yet. Update ArtMoon to 3.2.0 and open a paired host — it will request access here."
                                       Style="{StaticResource ST_RowDescription}" TextWrapping="Wrap"/>
                        </Border>
                    </Border>
                </StackPanel>

            </StackPanel>
        </ScrollViewer>
    </Grid>

</Page>
