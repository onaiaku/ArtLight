<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- Stesso TFM del progetto WPF: garantisce accesso alle stesse API WinRT (Spatial Audio,
         Windows.Graphics.Imaging, ecc.) senza dipendenze WPF (UseWPF non è presente). -->
    <TargetFramework>net8.0-windows10.0.19041.0</TargetFramework>
    <EnableWindowsTargeting>true</EnableWindowsTargeting>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RuntimeIdentifier>win-x64</RuntimeIdentifier>
    <Platforms>x64</Platforms>
    <!-- RootNamespace invariato: i file spostati non richiedono modifiche agli using -->
    <RootNamespace>StreamTweak</RootNamespace>
    <AssemblyName>StreamTweak.Core</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <!-- CIM/WMI per NetworkManager e SpeedChanger -->
    <PackageReference Include="Microsoft.Management.Infrastructure" Version="3.0.0" />
    <!-- PDH PerformanceCounter per HostMetricsCollector -->
    <PackageReference Include="System.Diagnostics.PerformanceCounter" Version="8.0.0" />
    <!-- SQLite per GOG library scanner (Chrome/GOG database) -->
    <PackageReference Include="Microsoft.Data.Sqlite" Version="8.0.11" />
    <!-- Override the native SQLite bundle pulled transitively by Microsoft.Data.Sqlite (2.1.6,
         which ships SQLite < 3.50.2) to 3.0.3 — closes Dependabot alert GHSA-2m69-gcr7-jv3q /
         CVE-2025-6965 (SQLite aggregate-term memory corruption). Hygiene: the only SQLite use is
         a local read-only query of GOG Galaxy's own galaxy-2.0.db, not untrusted input. -->
    <PackageReference Include="SQLitePCLRaw.bundle_e_sqlite3" Version="3.0.3" />
  </ItemGroup>

  <ItemGroup>
    <!-- NVIDIA Sentinel value-label catalog — sourced from nvidiaProfileInspector
         (github.com/Orbmu2k/nvidiaProfileInspector), MIT © Orbmu2k. Used by
         NvidiaSettingCatalog to map raw DRS values to friendly labels (e.g. 2 → "Medium"). -->
    <EmbeddedResource Include="Resources\Npi\CustomSettingNames.xml" />
    <EmbeddedResource Include="Resources\Npi\LICENSE.NPI.txt" />
  </ItemGroup>

</Project>
